---
description: 项目编码风格约束
globs: 
alwaysApply: true
---

# 编码风格

## Emoji 禁止

**未经用户明确允许，禁止在任何地方使用 emoji。** 包括代码、注释、文档、日志输出。

## 语言

- 使用中文交流
- 代码中禁止中文变量名
- 所有专业术语后面必须跟中文说明（在文档和注释中）

## 命名规范

### TypeScript

- 模块/函数：`camelCase`
- 类/接口/类型：`PascalCase`
- 常量：`UPPER_SNAKE_CASE`
- 文件名：`kebab-case.ts`
- 使用 `type` 优先于 `interface`
- 严格模式 `strict: true`
- 布尔变量用 `is`、`has`、`can` 前缀

### Python

- 模块/函数：`snake_case`
- 类：`PascalCase`
- 常量：`UPPER_SNAKE_CASE`
- 类型注解：必须使用（Python 3.12+）
- 布尔变量用 `is_`、`has_`、`can_` 前缀

### 通用

- 禁止无意义缩写（允许业界标准：API、URL、ID、DB、CLI、SDK）
- 变量/函数命名要自解释，不依赖注释

## 注释原则

- 只写"为什么"，不写"做了什么"
- 代码能自解释的不加注释
- 复杂算法或非直觉逻辑必须注释

## 导入规则

- 在使用处必须确保有对应 import
- 不要假设外层已导入

## 错误处理

- 所有异步操作必须处理错误
- 禁止吞掉错误不上报
- 统一错误格式（见 AGENTS.md）

## 回答准则

- 先查证再开口：不确定的事情先查证，查不到就说查不到
- 明确依据来源：回答时说明依据（哪个文件、哪行代码）
- 不知道就是不知道：列出做了哪些尝试
- 出错就认：说错了直接承认

### 禁止

- 禁止猜测性断言（"应该是这样"）
- 禁止想当然（"一般项目都这样"）
- 禁止半吊子回答（查一半就急着回答）
- 禁止信息编造（没验证过的当作事实）

## 问题处理流程

**发现问题后不要立即动手修，先报告给用户，等确认后再改。**

1. 明确描述问题、影响范围、根因
2. 提出建议方案（可多个），说明优缺点
3. 等待用户确认
4. 确认后执行

例外（可直接修）：明显拼写错误、导入缺失、lint 错误、用户明确说"直接修"。
